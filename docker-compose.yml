version: "3.3"
services:
  db:
    image: "mcr.microsoft.com/mssql/server:2017-latest"
    ports:
      - 1433:1433
    volumes:
      - firewatch-db-data:/var/opt/mssql
    environment:
      SA_PASSWORD: "Your_password123"
      ACCEPT_EULA: "Y"

volumes:
  firewatch-db-data:
